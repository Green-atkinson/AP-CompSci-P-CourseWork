#include <stdio.h>
#include <cs50.h>

int digit_sum(int num);

int main(void)
{
    printf("digit_sum(15) should be 6:%i\n", digit_sum(15));
    printf("digit_sum(97) should be 16:%i\n", digit_sum(97));
}

//Precondition: num is a 2-digit number
//Postcondition: returns the sum of the two digits
//               for example digit_sum(45) returns 9.
int digit_sum(int num)
{
    //TODO
}